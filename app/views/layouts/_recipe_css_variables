<% content_for(:style) do %>

  <% @recipes.each do |r| %>
    .<%= r.name %>-<%= r.id %> {
      background-image: url('<%= r.photo_url.url %>'); 
    }
  <% end %>

  <% @events.each do |e| %>

   <% if e.get_recipe(e.recipe_id) %>
     .<%= e.recipe_name %>-<%= e.recipe_id %> {
       background-image: url('<%= e.get_recipe(e.recipe_id).photo_url.url %>'); 
     }
    <% end %>
  <% end %>




  
     
  
<% end %>