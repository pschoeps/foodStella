<% if @events.length != 0 %>
 [
  <% @events.each do |e| %>
    {  
       "id": "<%= e.id %>", 
       "start": "<%= e.start_at %>", 
       "end": "<%= e.end_at %>", 
       "title": "<%= e.get_recipe(e.recipe_id).get_friendly_name %>id-<%= e.recipe_id %>", 
       "className": "<%= e.get_recipe(e.recipe_id).get_friendly_name %>id-<%= e.recipe_id %>" 
     }<% unless e == @events.last %>,<% end %> 
  <% end %>
  ]
<% end %>
